# Define a Python Function

Python functions use the shared handler contract documented in [Define a Python Function](../define-function.md).

## Python-specific details

The `python` kind uses the common handler arguments and output collection rules without adding runtime-specific arguments. Use the shared page for handler definition, reserved arguments, inputs, parameters, outputs, and init functions.
