# Container image object

The `Containerimage` object provides the standard entity CRUD methods.

## CRUD methods

- `save()`: Save or update the container image in the backend.
- `export()`: Export the container image locally as a YAML file.
- `refresh()`: Refresh the container image from the backend.
